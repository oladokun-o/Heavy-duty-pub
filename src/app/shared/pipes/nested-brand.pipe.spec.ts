import { NestedBrandPipe } from './nested-brand.pipe';

describe('NestedBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
